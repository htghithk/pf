---
interface Props {
    title: string;
    link: string;
    bold: boolean;
}

const { title, link, bold } = Astro.props;
const Element = link != null ? 'a': 'p'
---

<Element href={link} class=`font-mono hover:underline ${bold && 'font-bold'}`>
    {title}
</Element>
