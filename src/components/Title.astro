---
const {title, smalltitle} = Astro.props
---

<header class="flex flex-col gap-4 items-start mt-0 border-b pb-1 dark:border-gray-100 border-black">
    <h1 class="font-mono text-5xl force-wrap sm:px-4 px-0">
        {title}
    </h1>
    {smalltitle && <h2 class="font-mono text-xl px-6">
      {smalltitle}
    </h2>}
</header>

<style>
/* Force wrapping of very long words/URLs while keeping normal wrapping otherwise */
.force-wrap {
  overflow-wrap: anywhere; /* modern, breaks words where needed */
  word-wrap: break-word;   /* legacy */
  word-break: break-word;  /* fallback (some engines) */
  -ms-word-break: break-all; /* IE fallback */
  hyphens: auto;           /* allow hyphenation where supported */
}
</style>